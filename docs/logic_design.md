調理補助アプリ_stepの成立ロジック考察メモ

メニュー
[
  材料
    {
      素材
      量
      (単位)
      (サブレシピ参照)
    }
  手順
    {
      id
      手順概要
      前提
      ながら可能か
      優先順位
      メモ
    }
]

>>肉じゃが（煮込み15分・放置可）」と「ほうれん草の胡麻和え（作業5分）」を同時に作る時、あなたならどういう順番で、何をトリガーに動きますか？
<<肉じゃが(A)>>
1:野菜を切る
2:油を入れ、肉を炒める
3:野菜を2に合流し、炒める
4:煮汁の材料を入れ沸騰
5:灰汁を取る
6:落とし蓋をし20分煮る
7:火を止め、10分蒸らす

<<ほうれん草の胡麻和え(B)>>
1:ゴマや出汁をすりこぎで混ぜとく
2:塩を入れた水を沸騰させる
3:ほうれん草をさっと茹でる
4:冷水をくぐらせる
5:よく絞る
6:1と和える

<<回答>>
A1A2A3A4A5A6B1B2B3B4B5B6A7

じゃあjsonのSTEPSを、どうソートするか。
<<dependsOnに従った単メニューの並べ替え>>
1.dependsOnがないstepを探す(=最初だから)
→sorted[0]にする
2.そのstepをdependsOnにしてるstepを探す、sortedに足す
3.2のstepをdependsOnにしてるstepを探す
4.3を繰り返す
~~⭐︎dependsOnのないstepが複数あった場合の優先順位はどうなる？~~
~~ほうれん草の場合、step1、2ともに参照元なし。~~
~~参照先はそれぞれあり、step1は最後に回る。~~
~~step1の参照先を探す→step7~~
~~step7の参照元探す→6,5,4を辿ることになる....そしてstep2へ行き着く~~
~~1と6が合流する7の処理が大切で、...??~~

//トポロジカルソートを踏襲・理解の確認
1.dependsOnがないstepを探す(=入次数が0の頂点)
2.配列に追加する(その頂点をキューに入れる)
3.そのstepに依存しているstepを探し、そのdependsOnから1のidを削除する(その頂点とそこから伸びる点を削除する)
4.1に戻る
→削除、ではなくループの判定で、辺の削除に相当するSetを用意する

04.01
一週間ぶりになってしまいました。昨日・一昨日は絶対作業できたはずなのに。
今日は赤煉瓦倉庫(エンジニアカフェ)で作業。
今できてること：一つのレシピ内で、トポロジカルソートにより、依存順のみを反映したソートができる。
今日の目標：
0. 病まないこと
1. isPassiveのフラグを反映した優先順位をつけること
2. 複数レシピを混ぜて、1を反映したソートができること

例えば、
A1：具材を切る
A2：沸騰させる
A3：具材を入れる(煮込む)[A1,A3]
(中略)
A6：ミソだれを混ぜる
A7：弱火にしたとこにA6を混ぜ、ごにょごにょ[A5,A6]
の場合、A2/A3はisPassiveである。
でも、A3は入次数は0じゃない。
A2とA1の優先度は同じくらいな気はする。
煮込んでる間に、A6はできる。

一旦、以下のような実装をする。
・入次数0のキューを作る
・その中で、最も優先度が高いものをsortedに入れる

⭐︎いったん、複数メニューとisPassiveを反映したソーティングができるようになった。
ここで、実際の料理手順を考えた時に「立て続けタスク」(StepAが終わったらすぐしなければならないStepB)などの管理をした方がいい気がしてきたので、
RawStepとStepというtypeの使い分けや、Stepに持たせるisChainフラグを反映したBlock単位でのソートを目指したい気持ち。

レシピの一覧与える
レシピの中のStepをRawからStepに変換する
(ここで全レシピをバラしたStepのオブジェクトにする？)
ソートプログラム呼び出し(引数：レシピの配列もしくはStepバラしたオブジェクト)
戻り値(ステップ or ブロックの配列)を表示？